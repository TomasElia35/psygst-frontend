# Entorno de Desarrollo Local (Vite usará esto automáticamente con 'npm run dev')
# Apunta a tu backend de Spring Boot corriendo en local (localhost:8080)

VITE_API_URL=http://localhost:8080/api/v1
VITE_MAINTENANCE_MODE=false
